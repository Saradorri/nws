# Notification web socket service
Send notification from rabbitMQ to dashboard 
 
## Requirements

- `GoLang 1.14.2` or higher

#### `Golang` packages:
`Golang` packages are available in `go.mod` file (use `go mod vendor` and `go mod tidy`)